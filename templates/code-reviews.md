# Code reviews

> Append one entry per PR. Header MUST start `## PR #<N>` (the merge gate greps `^## PR #<N>`).
> End each entry with `VERDICT: APPROVED @<HEAD-short-sha>` (or CHANGES-REQUESTED / NEEDS-FIXES).
> This file is also an autoloop activation marker — its presence enables the gates in this project.
